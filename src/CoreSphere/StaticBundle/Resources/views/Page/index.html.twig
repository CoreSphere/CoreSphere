{% extends 'CoreSphereStaticBundle:Page:layout.html.twig' %}

{% block title %}
{{ 'headline.page_index'|trans }}
{% endblock %}

{% block content %}
  <h2>{{ 'headline.page_index'|trans }}</h2>
	<table class="records_list">
			<thead>
					<tr>
							<th>{{ 'attribute.page_id'|trans }}</th>
							<th>{{ 'attribute.page_title'|trans }}</th>
							<th>{{ 'attribute.page_content'|trans }}</th>
							<th>{{ 'attribute.page_created_at'|trans }}</th>
							<th>{{ 'attribute.page_published_at'|trans }}</th>
							<th>{{ 'attribute.page_updated_at'|trans }}</th>
							<th>{{ 'attribute.page_permalink'|trans }}</th>
							<th>{{ 'attribute.page_actions'|trans }}</th>
					</tr>
			</thead>
			<tbody>
			{% for entity in entities %}
					<tr>
							<td><a href="{{ path('page_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
							<td>{{ entity.title }}</td>
							<td>{{ entity.content }}</td>
							<td>{{ entity.createdat|date('Y-m-d H:i:s') }}</td>
							<td>{{ entity.publishedat|date('Y-m-d H:i:s') }}</td>
							<td>{{ entity.updatedat|date('Y-m-d H:i:s') }}</td>
							<td>{{ entity.permalink }}</td>
							<td>
									<ul>
											<li>
													<a href="{{ path('page_show', { 'id': entity.id }) }}">{{ 'action.page_show'|trans }}</a>
											</li>
											<li>
													<a href="{{ path('page_edit', { 'id': entity.id }) }}">{{ 'action.page_edit'|trans }}</a>
											</li>
									</ul>
							</td>
					</tr>
			{% endfor %}
			</tbody>
	</table>
	
	<ul>
			<li>
					<a href="{{ path('page_new') }}">{{ 'action.page_new_page'|trans }}</a>
			</li>
	</ul>
{% endblock %}